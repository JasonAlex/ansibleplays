---#AWS
- gather_facts: False
  tasks:
    - name: "VPC Info"
      ec2_vpc_net_facts:
        profile: genpact-us-dev
        region: us-east-1
      register: vpc
      
    - debug:
      msg: {{vpc}}
