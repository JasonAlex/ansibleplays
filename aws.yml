--- #AWS
- name: VPC Demo
  hosts: all
  become: yes
  tasks:
    - ec2_vpc_net_facts:
        region: us-east-1
        profile: genpact-us-dev
      register: vpc
    - debug:
        msg: "{{vpc}}"
